<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <%= link_to 'Atomic Kitten', root_path, class:"navbar-brand" %>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <%= link_to 'Home', root_path, class:"nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Galerie de photos', root_path, class:"nav-link" %>
                    </li>
                    <% if current_user && current_user.admin %>
                    <li class="nav-item">
                        <%= link_to 'Rajouter une photo', new_item_path, class:"nav-link" %>
                    </li>
                    <% end %>

                    <% if user_signed_in? %>
                    
                        <li class="nav-item">
                            <%= link_to 'Modifier le profil', edit_user_registration_path, class:"nav-link" %>
                            </li>
                        <li class="nav-item">
                            <%= link_to 'Mon Panier', cart_path(current_user.cart.id), class:"nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to 'Déconnection', destroy_user_session_path, :method=>'delete', class:"btn btn-danger"%></a>
                        </li>

                    <% else %>

                        <li class="nav-item">
                            <%= link_to 'Inscription', new_user_registration_path, class:"btn btn-secondary", id:"inscriptionbutton" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to 'Connection', new_user_session_path, class:"btn btn-success" %>
                        </li>

                    <% end %>

                </ul>
            </div>
    </div>
</nav>
<br/>